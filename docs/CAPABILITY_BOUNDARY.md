# 能力边界

本系统输出研究辅助型操作建议，但不保证收益，不自动下单，不连接券商或实盘交易接口。

系统输出只用于研究辅助。公开数据可能延迟、缺失或错误；AKShare 接口也可能因为网络、源站变化或限流而失败。

OCR 结果可能有错字、漏字和页码错位。书籍策略只有在有明确来源时才能进入正式判断；低置信内容只保存在复核笔记中。

新闻解释可能不完整，事件影响只分为事实、解释、猜测和待验证事项。最终结论必须给明确操作建议：买入、卖出、加仓、减仓、持有、等待或补数据；`继续深挖`、`放入观察`、`暂时剔除`、`信息不足` 只作为辅助研究分级。
